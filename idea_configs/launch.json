{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debbuger for XDebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}/docker-woo/src"
            },
            "log": true
        }
    ]
}